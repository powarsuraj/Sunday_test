select * from employee order by EDEPT desc

select max(esal) as maxsal from employee

select max(EDEPT) as maxvalue from employee

select Count(*) as RecordCount from employee

<br>
select Count(*) as RecordCount from employee
